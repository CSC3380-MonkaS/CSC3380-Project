To run this program, you need to run only the "main.py" file.

Input files can be changed out, but must maintain the same name as those in the input_files directory

The generated output user file will appear in the output_files directory each time the program is run